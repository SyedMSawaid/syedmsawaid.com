<div class="py-6 border-t border-slate-100">
  <a href="{{ url }}" target="_blank" rel="noopener" class="text-slate-900 text-base leading-relaxed hover:text-slate-500 transition-colors duration-150">{{ title }}</a>
  <p class="text-xs text-slate-400 mt-1 mb-0">{{ show }}{% if year %} · {{ year }}{% endif %}</p>
</div>
