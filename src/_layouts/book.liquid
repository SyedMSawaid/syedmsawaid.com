---
layout: default
---

<div class="flex justify-center gap-2">
  <a href="/books">Books</a>
  <span>▸</span>
  <strong>{{ page.title }}</strong>
</div>

<div class="flex justify-center items-center gap-2 py-10">
  <h1 class="text-6xl font-bold">{{ page.title }}</h1>
</div>

{{ content }}