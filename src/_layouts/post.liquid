---
layout: default
---
<div class="mb-10">
  <h1 class="m-0 text-4xl font-bold text-left">{{ data.title }}</h1>

  <div class="flex text-xl gap-x-2">
    <div>{{ data.date | date: '%B %d, %Y' }}</div>
    <div>·</div>
    <div>{{ page.content | reading_time }} minutes</div>
  </div>
</div>

{{ content }}
